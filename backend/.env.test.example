# Test environment for the Vitest integration suite. Copy to .env.test to override.
# Defaults (used when this file is absent) point at a local brainbox_test DB.
DATABASE_URL=postgres://brainbox:brainbox@localhost:5432/brainbox_test
AUTH_SECRET=test-secret-not-for-production-000000000000000=
