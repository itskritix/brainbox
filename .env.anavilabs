# BrainBox TEST Configuration for AnAvi Labs
# Test deployment at: brainbox.anavilabs.com

# =============================================================================
# FRONTEND TEST CONFIG
# =============================================================================

# Server domain (where your backend API is accessible)
VITE_SERVER_DOMAIN=brainbox.anavilabs.com

# =============================================================================
# BACKEND TEST CONFIG
# =============================================================================

# Server settings
SERVER_NAME="BrainBox Test - AnAvi Labs"
SERVER_CORS_ORIGIN=https://brainbox.anavilabs.com

# Basic database (use simple/free tier for testing)
POSTGRES_URL=postgresql://test_user:test_pass@localhost:5432/brainbox_test

# Basic Redis (use local or free tier)
REDIS_URL=redis://localhost:6379/0

# Basic storage (use local MinIO or free S3 tier)
STORAGE_S3_ENDPOINT=http://localhost:9000
STORAGE_S3_ACCESS_KEY=minioadmin
STORAGE_S3_SECRET_KEY=minioadmin
STORAGE_S3_BUCKET=brainbox-test
STORAGE_S3_REGION=us-east-1
STORAGE_S3_FORCE_PATH_STYLE=true

# Email disabled for testing
SMTP_ENABLED=false

# =============================================================================
# DEPLOYMENT ARCHITECTURE
# =============================================================================

# Frontend: https://brainbox.anavilabs.com
# Backend:  https://brainbox.anavilabs.com/api (reverse proxy)
# 
# Your nginx/apache should proxy:
# https://brainbox.anavilabs.com/api/* → http://localhost:3000/*