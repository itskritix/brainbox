{{- if .Values.brainbox.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "brainbox.fullname" . }}
  labels:
    {{- include "brainbox.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "brainbox.fullname" . }}
  minReplicas: {{ .Values.brainbox.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.brainbox.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.brainbox.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.brainbox.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.brainbox.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.brainbox.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
