{{- if .Values.brainbox.ingress.enabled -}}
{{- if and .Values.brainbox.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.brainbox.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.brainbox.ingress.annotations "kubernetes.io/ingress.class" .Values.brainbox.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ include "brainbox.fullname" . }}
  labels:
    {{- include "brainbox.labels" . | nindent 4 }}
  {{- with .Values.brainbox.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.brainbox.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.brainbox.ingress.className }}
  {{- end }}
  {{- if .Values.brainbox.ingress.tls }}
  tls:
    {{- range .Values.brainbox.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.brainbox.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ include "brainbox.fullname" $ }}
                port:
                  name: http
              {{- else }}
              serviceName: {{ include "brainbox.fullname" $ }}
              servicePort: http
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
