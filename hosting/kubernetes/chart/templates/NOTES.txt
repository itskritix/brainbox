Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

═══════════════════════════════════════════════════════════════
ACCESSING BRAINBOX
═══════════════════════════════════════════════════════════════

{{- if .Values.brainbox.ingress.enabled }}
Your Brainbox instance will be available at:
{{- range .Values.brainbox.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.brainbox.ingress.tls }}s{{ end }}://{{ .host }}{{ if ne .path "/" }}{{ .path }}{{ end }}
  {{- end }}
{{- end }}

Note: Make sure your ingress controller is configured and DNS is pointing to your cluster.
{{- else }}
To access Brainbox, run:
  kubectl port-forward svc/{{ include "brainbox.fullname" . }} 3000:3000 -n {{ .Release.Namespace }}

Then visit: http://localhost:3000
{{- end }}

═══════════════════════════════════════════════════════════════
DESKTOP APPLICATION
═══════════════════════════════════════════════════════════════

Download the Brainbox desktop app from: https://brainbox.com/downloads
Use the server URL above when connecting.

═══════════════════════════════════════════════════════════════
DEPLOYMENT STATUS
═══════════════════════════════════════════════════════════════

Check deployment status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

View application logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "brainbox.name" . }} -f

═══════════════════════════════════════════════════════════════
SERVICES INFORMATION
═══════════════════════════════════════════════════════════════

{{- if .Values.postgresql.enabled }}
📊 PostgreSQL (with pgvector): {{ include "brainbox.postgresql.hostname" . }}:5432
{{- end }}
{{- if .Values.redis.enabled }}
🔄 Redis/Valkey: {{ include "brainbox.redis.hostname" . }}:6379
{{- end }}
{{- if .Values.minio.enabled }}
💾 MinIO Storage: {{ include "brainbox.minio.hostname" . }}:9000
   Console: {{ include "brainbox.minio.hostname" . }}:9001
{{- end }}

All service credentials are auto-generated and stored in Kubernetes secrets.
To retrieve them: kubectl get secrets -n {{ .Release.Namespace }}

═══════════════════════════════════════════════════════════════

For more information, visit: https://github.com/brainbox/brainbox
For support, create an issue at: https://github.com/brainbox/brainbox/issues
