# Environment Variables for Web App
# Copy this file to .env.local for local development

# Server Configuration (optional - will auto-detect if not specified)
# VITE_SERVER_DOMAIN=localhost:3000

# Examples for different deployment scenarios:
# For API subdomain: VITE_SERVER_DOMAIN=api.yourdomain.com
# For same domain: VITE_SERVER_DOMAIN=yourdomain.com
# For different port: VITE_SERVER_DOMAIN=yourdomain.com:3000
