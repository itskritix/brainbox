# =============================================================================
# BRAINBOX DEPLOYMENT CONFIGURATION
# Copy this file to .env.local and configure for your deployment
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------

# Option 1: Specify exact server domain (recommended for production)
# VITE_SERVER_DOMAIN=api.yourdomain.com
# VITE_SERVER_DOMAIN=yourdomain.com:3000

# Option 2: Let the app auto-detect based on frontend domain
# Frontend: app.yourdomain.com → Backend: api.yourdomain.com
# Frontend: yourdomain.com → Backend: yourdomain.com:3000

# -----------------------------------------------------------------------------
# DEPLOYMENT EXAMPLES
# -----------------------------------------------------------------------------

# Example 1: Separate API subdomain (recommended)
# Frontend deployed to: https://app.yourdomain.com
# Backend deployed to: https://api.yourdomain.com
# Configuration: VITE_SERVER_DOMAIN=api.yourdomain.com

# Example 2: Same domain with reverse proxy
# Frontend: https://yourdomain.com
# Backend: https://yourdomain.com/api (handled by nginx/reverse proxy)
# Configuration: VITE_SERVER_DOMAIN=yourdomain.com

# Example 3: Different port (development/staging)
# Frontend: http://yourdomain.com:4000
# Backend: http://yourdomain.com:3000
# Configuration: VITE_SERVER_DOMAIN=yourdomain.com:3000

# -----------------------------------------------------------------------------
# DEVELOPMENT (no configuration needed - uses localhost:3000 automatically)
# -----------------------------------------------------------------------------